# backend-ai

FastAPI service for image indexing/search logic in the VisualSearchEngine project.

## Scope

- This folder contains only the AI backend implementation.
- Project-wide Docker setup is documented at workspace root in `README.md`.
- Preferred compose file for full stack is `../docker-compose.yml`.

## Local health endpoint

- `http://localhost:8000/health`


